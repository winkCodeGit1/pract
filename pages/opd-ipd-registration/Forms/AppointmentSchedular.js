/** @format */

import MainHeader from 'layouts/main/MainHeader';

const AppointmentSchedular = () => {
  return <MainHeader isStaffLoginVal={false} />;
};

export default AppointmentSchedular;
